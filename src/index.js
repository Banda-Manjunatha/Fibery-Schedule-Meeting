import React from "react"
import ReactDOM from "react-dom/client" // Using ReactDOM for React 18
import App from "./App"

const root = ReactDOM.createRoot(document.getElementById("root"))
root.render(<App />)
